# Installation

No installation is required.

Download or clone the repository and run the scripts from SQL Server Management Studio or Azure Data Studio.

Required permissions:

```sql
GRANT VIEW SERVER STATE TO [YourLoginOrRole];
```

For database-level details, `VIEW DATABASE STATE` may also be required.
